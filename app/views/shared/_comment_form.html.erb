<%= form_for(@comment) do |f| %>
	<%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Compose new comment...",size: "30x3" %>
  </div>
  <%= f.hidden_field :story_id, value: story %>
  <%= f.submit "Post", class: "btn" %>
<%end%>