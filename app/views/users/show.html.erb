<%= provide(:title, @user.name) %>
<div class = "row">
	<aside class = "span4">
		<section>
			<h1>
				<%= @user.name %><br>
				<%= @user.email %><br>
				<%= @user.phone unless @user.phone.nil? || @user.phone.empty? %><br>
				<%= @user.position %>
			</h1>
		</section>
	</aside>
</div>

